import { SERVERS_LOOKUP } from "../utils/env";

// TODO: remove this from general, just for debugging
export const lookup = () => console.log(SERVERS_LOOKUP);

// TODO: remove this from general, just for debugging
export const woo = (_one: string, _two: string) => console.log(SERVERS_LOOKUP);
